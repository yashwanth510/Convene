# Cloudflare Pages build-time variable; this URL is public, not a secret.
# For local Vite development omit it; Vite proxies /api to localhost:8001.
VITE_API_BASE=https://YOUR-RENDER-SERVICE.onrender.com/api
