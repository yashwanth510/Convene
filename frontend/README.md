# Frontend

React + TypeScript + Vite UI for Convene.

For install, env setup, and how to run both backend and frontend, see the **[root README](../README.md)**.

```bash
npm ci
npm run dev
```

Dev server: http://localhost:5173 (expects API at http://localhost:8001)
